# Build Mode

- Implement only the requested change
- Edit only files allowed by the active task
- Prefer existing repo patterns and types
- Do not refactor unrelated code
- Do not add env vars, dependencies, or lockfile changes without approval
- Run targeted verification that matches the task scope
