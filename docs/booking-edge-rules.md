# Booking Edge Rules

This content was consolidated into `docs/booking.md`.

Keep this file as a short pointer for older references. Update `docs/booking.md` for current booking validation, cap, and edge-case rules.
